<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.mapper.AuthMapper">

    <insert id="insertUser" parameterType="org.scoula.domain.Auth.vo.UserVo">
        INSERT INTO user (
            username, password, name, nickname, phone_number, email,
            created_at, updated_at, risk_type, provider, total_credit
        ) VALUES (
                     #{username}, #{password}, #{name}, #{nickname}, #{phoneNumber}, #{email},
                     NOW(), NOW(), #{riskType}, #{provider}, #{totalCredit}
                 )
    </insert>

    <insert id="insertUserFromKakao" parameterType="org.scoula.domain.Auth.vo.UserVo">
        INSERT INTO user (
            username, nickname, provider, risk_type, total_credit, created_at, updated_at
        ) VALUES (
                     #{username}, #{nickname}, #{provider}, #{riskType}, #{totalCredit}, NOW(), NOW()
                 )
    </insert>

    <select id="existsByUsername" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM user WHERE username = #{username}
    </select>

    <select id="existsByNickname" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM user WHERE nickname = #{nickname}
    </select>

    <select id="findByUsernameAndPassword" resultType="org.scoula.domain.Auth.vo.UserVo">
        SELECT * FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="findByUsername" parameterType="String" resultType="org.scoula.domain.Auth.vo.UserVo">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findByEmail" resultType="org.scoula.domain.Auth.vo.UserVo">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="findByNameAndEmail" resultType="org.scoula.domain.Auth.vo.UserVo">
        SELECT * FROM user WHERE name = #{name} AND email = #{email}
    </select>

    <update id="updatePassword">
        UPDATE user SET password = #{newPassword} WHERE id = #{userId}
    </update>

    <select id="countByNickname" resultType="int">
        SELECT COUNT(*) FROM user WHERE nickname = #{nickname}
    </select>

</mapper>
