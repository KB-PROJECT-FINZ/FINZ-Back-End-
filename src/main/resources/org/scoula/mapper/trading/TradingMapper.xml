<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.mapper.TradingMapper">

    <select id="getUserTransactions" resultType="org.scoula.domain.trading.dto.TransactionDTO">
        SELECT
            t.stock_code as stockCode,
            t.stock_name as stockName,
            t.quantity,
            t.total_amount as totalAmount,
            t.transaction_type as transactionType
        FROM transactions t
                 JOIN user_accounts ua ON t.account_id = ua.account_id
        WHERE ua.user_id = #{userId}
        ORDER BY t.executed_at DESC
            LIMIT 10
    </select>

</mapper>